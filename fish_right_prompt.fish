function fish_right_prompt -d "Write out the right prompt of the lazy theme"
  # set -l colnormal (set_color normal)
  # set -l ps_where $colnormal(whoami)@(hostname|cut -d . -f 1)
  
  # # Right Prompt

  # if test $__lazy_display_rprompt -eq 1
  #   echo -n -s $ps_where
  # end
end
